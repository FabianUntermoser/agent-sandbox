# Minimal tmux config for sandbox
set -g default-terminal "tmux-256color"
set -ga terminal-overrides ",*256col*:Tc"
set -g mouse on
set -g history-limit 5000
set -g escape-time 0
set -g base-index 1
setw -g pane-base-index 1
